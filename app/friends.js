import React from 'react';
import { StyleSheet, Text, View, Image, Switch, SafeAreaView, FlatList, TouchableOpacity, Modal } from "react-native";
import { Link, Stack } from "expo-router";

export default function Page() {
    return(
        <View>
            <Text>Friends Page</Text>
        </View>
    );
}